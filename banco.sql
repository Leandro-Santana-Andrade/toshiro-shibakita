CREATE TABLE dados (
    Matricula int not NULL,
    Nome varchar(50),
    Sobrenome varchar(50),
    DataNascimento date,
    Endereco varchar(150),
    Cidade varchar(50),
    Host varchar(50),
    PRIMARY KEY (Matricula)
);
